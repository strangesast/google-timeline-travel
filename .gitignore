# ---- Raw Google Takeout / Timeline data (FULL PII — never commit) ----
location-history.json
takeout-*.tgz
takeout-*.zip
Takeout/
*.mbox

# ---- Intermediate exports (exact dates/times + precise coordinates) ----
trips_geo.json
trips_geo_matched.json
all_trips_geo.json
road_trips.json
road_trips.csv

# ---- Data tiers: dev = PII (never commit); public = redacted (committed, hosted) ----
src/data/*.dev.json

# ---- Build artifacts ----
dist/
map_viewer.html
all_trips.html
*.dev.html

# ---- Node / Python tooling ----
node_modules/
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
dist-dev/
