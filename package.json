{
  "name": "timeline-road-trips",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "data": "python3 prepare_data.py",
    "dev": "vite --mode dev",
    "prebuild": "python3 check_public.py",
    "build": "vite build",
    "build:dev": "vite build --mode dev",
    "preview": "vite preview"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
